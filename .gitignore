*.iml
.gradle/
.kotlin/
/local.properties
/.idea/
.DS_Store
/build
*/build
**/build/
/captures
.externalNativeBuild
.cxx
local.properties
*.keystore
!debug.keystore
/app/release/

# Real manga pages used to judge V2 quality. Supplied locally, never committed:
# it is somebody else's copyrighted work (docs/milestones/v2.md).
/comic-sample/

# Model weights, fetched locally and never committed — same rule as the manga
# pages: somebody else's artefact, and far too large for a repository.
/models/

# JVM crash dumps. This machine runs Gradle beside an emulator and occasionally
# runs out of memory mid-build; the dump lands wherever the worker was.
hs_err_pid*.log
replay_pid*.log
