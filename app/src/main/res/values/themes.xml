<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose owns the real styling; this only keeps the window chrome out. -->
    <style name="Theme.Babel" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
